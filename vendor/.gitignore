# Ignore cruft from configure
config.cache
config.log
config.status

# Ignore compiler-generated intermediate files
*.o
*.a
*.hi
*.p_hi
*.p_o
lmstrip
tclsh

# Ignore generated libraries
libyices.so
libyices.so.*
libstp.so
libstp.so.*

# Don't ignore vendor libraries and objects that really are checked in
!lmnewgen.o
!libcrvs.a
!liblmgr.a
!liblmgr_nomt.a
!liblmgr_pic.a
!libsb.a
!libcrvs_pic.a
!liblmgr_as.a
!liblmgr_nomt_pic.a
!liblmgr_s.a
!libsb_pic.a

iverilog/obj-linux32
iverilog/obj-linux64
